# wfkit Docs

This page is published to the Webflow page with slug `docs`.

## How it works

- Edit markdown in `docs/index.md`
- Run `wfkit docs`
- Open the `docs` page in Webflow

## Tips

- Add headings to build a docs table of contents automatically
- Use fenced code blocks for commands and examples
- Add links with standard markdown syntax

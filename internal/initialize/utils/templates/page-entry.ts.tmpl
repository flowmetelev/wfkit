import { definePage, mountFeature } from '@/utils/webflow'

definePage('home', pageRoot => {
  mountFeature('home-root', '[data-wf-home-root]', element => {
    element.setAttribute('data-page-ready', 'true')
  }, pageRoot)

  // Mount home-only features here.
})

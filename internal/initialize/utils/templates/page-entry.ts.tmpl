import { definePage, mountPageRootFeature } from '@/utils/webflow'

definePage('home', pageRoot => {
  mountPageRootFeature('home', element => {
    element.setAttribute('data-page-ready', 'true')
  }, pageRoot)

  // Mount home-only features here.
})

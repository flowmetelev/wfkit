import { mountFeature } from '@/utils/webflow'

export function mountSiteStatusFeature() {
  mountFeature('site-status', '[data-wf-site-root], html', element => {
    element.setAttribute('data-wf-enhanced', 'true')
  })
}

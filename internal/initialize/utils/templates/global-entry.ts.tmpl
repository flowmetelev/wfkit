import { $, onReady } from '@/utils/dom'
import { definePage, mountFeature, mountGlobalFeature, mountPageRootFeature, onWebflowReady } from '@/utils/webflow'
import { mountSiteGlobal } from './modules/site.global'

export const WF = {
  $,
  definePage,
  mountFeature,
  mountGlobalFeature,
  mountPageRootFeature,
  onReady,
  onWebflowReady,
  mountSiteGlobal
}

if (typeof window !== 'undefined') {
  window.{{.GlobalVar}} = WF
}

mountSiteGlobal()
